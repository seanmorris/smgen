name: Build and Deploy

on: push

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - name: Get GitHub Pages URL
        id: get_pages_url
        run: echo "BASE_URL=$(gh api "repos/$GITHUB_REPOSITORY/pages" --jq '.html_url | rtrimstr("/")')" >> $GITHUB_ENV
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Checkout
        uses: actions/checkout@v4

      - name: Install SMGen Search
        run: npm i -g smgen-search

      - name: Install dependencies
        run: sudo apt-get update && sudo apt-get install -y pandoc php-cli php-yaml uuid

      - name: Install yq (Go binary)
        run: |
          wget -qO /usr/local/bin/yq https://github.com/mikefarah/yq/releases/download/${{ env.YQ_VERSION }}/yq_linux_amd64
          chmod +x /usr/local/bin/yq
        env:
          YQ_VERSION: v4.47.2

      - name: Run test suite
        run: |
          mkdir -p .ci-debug
          set -o pipefail
          bash ./tests/run.sh 2>&1 | tee .ci-debug/test-suite.log

      - name: Clear files
        run: rm -rf ./docs/*

      - name: Build site
        run: ./smgen.sh build

      - name: Upload failure diagnostics
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: ci-debug-artifacts
          if-no-files-found: ignore
          path: |
            docs/**
            .ci-debug/**
            .smgen.yaml
            .smgen-rc
            .github/workflows/build-and-deploy.yml
            tests/run.sh

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: './docs'

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

      - name: Show GitHub Pages URL
        run: |
          echo "GitHub Pages URL: ${{ env.BASE_URL }}"
